package components

// Adds a custom command to the database
func AddCommand(command string, value string) {

}

// Removes a custom command from the database
func RemoveCommand(command string) {

}

// Fetches the value of a custom command from the database
func GetCommand(command string) string {
	return ""
}
